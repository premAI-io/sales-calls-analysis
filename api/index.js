
const { handleAnalyze } = require('./analyze');
const { handleHealth } = require('./health');
const { handleModels } = require('./models');

module.exports = {
    handleAnalyze,
    handleHealth,
    handleModels
};